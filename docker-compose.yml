services:
  postgres:
    image: postgres:16-alpine
    container_name: logistics_db
    environment:
      POSTGRES_USER: logistics
      POSTGRES_PASSWORD: logistics123
      POSTGRES_DB: logistics_db
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U logistics -d logistics_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: logistics_backend
    env_file:
      - ./backend/.env
    ports:
      - "3001:3001"
    environment:
      DATABASE_URL: postgresql://logistics:logistics123@postgres:5432/logistics_db
      JWT_SECRET: super_secret_jwt_key_change_in_prod
      AI_SERVICE_URL: http://ai-service:8000
      PRISMA_ACCEPT_DATA_LOSS: "true"
      ROUTE_WEATHER_REFRESH_SECONDS: 300
      ROUTE_WEATHER_REFRESH_TICK_SECONDS: 60
      ROUTE_WEATHER_REFRESH_MAX_PER_TICK: 5
      PORT: 3001
    depends_on:
      postgres:
        condition: service_healthy

  ai-service:
    build:
      context: ./ai-service
      dockerfile: Dockerfile
    container_name: logistics_ai
    ports:
      - "8000:8000"
    environment:
      YANDEX_WEATHER_API_KEY: ${YANDEX_WEATHER_API_KEY:-}
      OPENWEATHER_API_KEY: ${OPENWEATHER_API_KEY:-}
      NEWS_API_KEY: ${NEWS_API_KEY:-}

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: logistics_frontend
    ports:
      - "3002:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:3001
      NEXT_PUBLIC_WS_URL: ws://localhost:3001
      NEXT_PUBLIC_AI_URL: http://localhost:8000
    depends_on:
      - backend

volumes:
  postgres_data:
  ai_models:
